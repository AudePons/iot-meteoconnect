import Vue from 'vue'
import Vuex from 'vuex'

import { devices } from './device.module'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    devices
  }
})
